
server:
  port: 8761

eureka:
  instance:
    hostname: localhost
  client:
#    关闭自己注册自己
    registerWithEureka: false
    fetchRegistry: false
    serviceUrl:
      defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka/